name: Security

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  secrets-pii:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Credential & PII check
        run: bash .github/scripts/pii-check.sh
